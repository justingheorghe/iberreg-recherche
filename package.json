{
  "name": "spanien-eigentuemer-recherche",
  "version": "1.0.0",
  "description": "Deutschsprachige Legal-Tech-Webanwendung zur rechtmaessigen Recherche offizieller spanischer Immobilieninformationen.",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "node src/server.js",
    "start": "NODE_ENV=production node src/server.js",
    "test": "node --test tests/*.test.js",
    "vercel-build": "cd react-ui && npm install && npm run build && cp -r dist ../public"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "pg": "^8.16.3"
  }
}
